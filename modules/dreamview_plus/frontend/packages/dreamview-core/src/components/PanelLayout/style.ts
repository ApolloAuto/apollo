import { makeStyles } from '@dreamview/dreamview-theme';

export default makeStyles((theme) => ({
    'layout-root': {
        position: 'relative',
        height: '100%',
        marginLeft: '-1px',
    },
}));
