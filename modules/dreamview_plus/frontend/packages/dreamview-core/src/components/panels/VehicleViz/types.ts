// 视角
export enum ViewType {
    DEFAULT = 'Default',
    OVERHEAD = 'Overhead',
    MAP = 'Map',
    NEAR = 'Near',
    // 2D视角
    TOP_DOWN = 'TopDown',
}

export enum VizCurMode {
    DEFAULT = 'Default',
    FOLLOW = 'Follow',
}
