# Caffe models, sum up to 300MB on 2018-10-17.
*.caffemodel filter=lfs diff=lfs merge=lfs -text
# TODO(all): Currently we have some models named as *.md which should be renamed and tracked.
# Camera test data which contains lots of images, sum up to 380MB on 2018-10-17
modules/perception/testdata/camera/** filter=lfs diff=lfs merge=lfs -text
