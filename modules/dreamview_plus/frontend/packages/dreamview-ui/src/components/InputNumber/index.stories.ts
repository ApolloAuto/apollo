import { Meta, StoryObj } from '@storybook/react';
import { Template } from './example';

const meta: Meta<typeof Template> = {
    title: 'Dreamview/InputNumber',
    component: Template,
    tags: ['autodocs'],
};

export const Primary = Template.bind({});

export default meta;
