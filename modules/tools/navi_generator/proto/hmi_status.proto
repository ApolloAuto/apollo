syntax = "proto2";

package apollo.navi_generator;

import "modules/monitor/proto/system_status.proto";

message HMIStatus {
  optional apollo.monitor.SystemStatus system_status = 1;
  optional string current_mode = 3 [default = "Standard"];
}
