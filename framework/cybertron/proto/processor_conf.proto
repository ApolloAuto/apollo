syntax = "proto2";

package apollo.cybertron.proto;

enum ProcessStrategy {
  CLASSIC = 0;
  CFS = 1;
}

message ProcessorConf {
  optional uint32 processor_num = 1;
  optional ProcessStrategy process_strategy = 2 [default = CFS];
}
