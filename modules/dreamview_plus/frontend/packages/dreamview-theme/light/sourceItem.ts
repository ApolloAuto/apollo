import { font, colors } from './base';

export default {
    color: font.color.colorInBackground,
    colorHover: font.color.colorInBackgroundHover,
    activeBgColor: colors.brand4,
    activeColor: font.color.colorInBackground,
    activeIconColor: colors.brand2,
    bgColor: colors.transparent,
    bgColorHover: colors.transparent1,
    disabledColor: '#A6B5CC',
};
